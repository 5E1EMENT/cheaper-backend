// Создаем базовый класс для чат-бота
export class ChatBot {
    async getUnifiedProductName(productName) {
        return `Response from ChatBot: ${input}`;
    }
    async getSimilarProductStrings(productsData, productName) {
        return [];
    }
}